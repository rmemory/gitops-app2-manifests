apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deleteme1-ingress
spec:
  rules:
  - http:
      paths:
      - path: /*
        pathType: ImplementationSpecific
        backend:
          service:
            name: deleteme1-service
            port:
              number: 80